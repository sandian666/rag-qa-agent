# ============================================================
# .gitignore —— 给 git 看的"黑名单"：列在这里的东西永远不进仓库
# 规则：一行一条；以 / 结尾 = 只挡文件夹；# 开头 = 注释
# ============================================================

# --- 1. 虚拟环境：整个 .venv 是 1.6 万个文件，进仓库会让仓库爆掉 ---
.venv/
venv/

# --- 2. Python 运行缓存：每次跑都会自动重新生成，没有保存价值 ---
__pycache__/
*.py[cod]

# --- 3. IDE 配置：PyCharm 的个人设置，和项目本身无关 ---
.idea/
.vscode/

# --- 4. 向量库：chroma_db 能由 create_db.py 重新生成，且体积大 ---
chroma_db/

# --- 5. 机密：API key 一旦进仓库 = 公开，会被盗刷 ---
.env
*.env
key.txt
apikey.txt

# --- 6. 操作系统垃圾文件 ---
Thumbs.db
desktop.ini
.DS_Store